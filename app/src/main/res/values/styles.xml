<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Login" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkblue</item>
        <item name="colorPrimaryDark">@color/statusbar_darkblue</item>
        <item name="colorAccent">@color/lightgreen</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/statusbar_darkblue</item>
    </style>

    <style name="AppTheme.Login2" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkblue</item>
        <item name="colorPrimaryDark">@color/statusbar_darkblue</item>
        <item name="colorAccent">@color/lightgreen</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/statusbar_darkgreen</item>
    </style>

    <style name="AppTheme.Home" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkblue</item>
        <item name="colorPrimaryDark">@color/statusbar_darkblue</item>
        <item name="colorAccent">@color/lightgreen</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/lightgreen</item>
    </style>

    <style name="ThemeOverlay.MyTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>
